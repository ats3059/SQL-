SELECT RE.N AS HOUR , IFNULL(COUNT(HOUR(DATETIME)),0) AS COUNT
FROM ANIMAL_OUTS AO
    RIGHT JOIN ( WITH RECURSIVE cte AS (
        SELECT 0 AS n
        UNION ALL
        SELECT n + 1 FROM cte WHERE n < 23
    )SELECT n FROM cte ) RE
    ON RE.N = HOUR(DATETIME)
GROUP BY RE.N;
